# ADK Documentation Crawler Configuration
# Copy this to .env.crawler and fill in your values
# Load with: export $(cat .env.crawler | xargs)

# Required: Google Cloud Project ID
GCP_PROJECT_ID=bobs-brain

# Optional: GCS bucket name (default: ${GCP_PROJECT_ID}-adk-docs)
BOB_DOCS_BUCKET=bobs-brain-adk-docs

# Optional: Service account email (for CI/CD only)
# CRAWLER_SA_EMAIL=crawler@bobs-brain.iam.gserviceaccount.com

# Optional: Crawl delay in seconds (default: 1.0)
# Adjust based on site's robots.txt and rate limiting needs
CRAWL_DELAY_SECONDS=1.0

# Optional: Maximum tokens per chunk (default: 1500)
# Adjust based on your embedding model's token limit
MAX_CHUNK_TOKENS=1500

# Optional: Starting URL for crawler (default: https://google.github.io/adk-docs/)
# ADK_DOCS_START_URL=https://google.github.io/adk-docs/

# Optional: Temporary directory for intermediate files (default: ./tmp)
# CRAWLER_TMP_DIR=./tmp

# Note: GCP credentials are loaded via application default credentials
# Run: gcloud auth application-default login
# Or set: GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
